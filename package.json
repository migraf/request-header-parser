{
  "name": "request-header-parser",
  "version": "1.0.0",
  "description": "parse the request headers to display operating system, language and ip adress",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Graf",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.1"
  }
}
